<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dzcoffee">

    <!-- Basic permissions (internet for Firebase/HTTP, network state checks) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DZ Café"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- Launcher activity (now MainActivity, not Splash) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Customer registration screen -->
        <activity
            android:name=".RegisterCustomerActivity"
            android:exported="false" />

        <!-- Optional: Home screen after login (placeholder, keep if you have it) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- If you previously had SplashActivity, it is intentionally removed -->
    </application>

</manifest>
